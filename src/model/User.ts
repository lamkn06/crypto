export interface User {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  notification: number;
}

export interface UserResponse {
  id: string;
  first_name: string;
  last_name: string;
  email: string;
  notification: number;
}
